{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "Classic Snake game with HTML5 Canvas, TypeScript, and unlockable skins",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "vitest": "^1.2.2"
  }
}
